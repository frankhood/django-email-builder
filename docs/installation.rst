============
Installation
============

At the command line::

    $ easy_install django-email-builder

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-email-builder
    $ pip install django-email-builder
